# Core-Polish — ProjectHub Core 정책

갱신일: 2026-09-26 (KST)

이 문서는 `src/ProjectHub.Core`의 장기 정책을 정의한다.

제1조 (책임)

① Core는 ProjectHub의 공통 도메인 모델, 상태 모델, 서비스 인터페이스와 저장소 계약을 정의한다.
② Core는 특정 UI, HTTP 호스트, 데이터베이스 제품, 파일시스템 구현 또는 AI 실행 방식의 책임을 맡지 않는다.

제2조 (의존성)

① Core는 다른 ProjectHub 하위 프로젝트에 의존하지 않는 것을 원칙으로 한다.
② Infrastructure, Server, Agent 등 다른 프로젝트가 사용할 공통 의미는 필요한 범위에서 Core의 모델과 계약으로 표현한다.

제3조 (도메인 경계)

① 도메인 규칙과 외부 시스템 호출 구현을 분리한다.
② Supabase, NAS, HTTP, 운영체제 경로와 같은 외부 구현 세부사항을 Core의 도메인 규칙으로 승격하지 않는다.
③ 공유 계약을 변경할 때는 해당 계약을 사용하는 프로젝트의 호환 영향을 함께 확인한다.

제4조 (검증)

① Core의 도메인 규칙 변경은 가능한 범위에서 단위 테스트로 검증한다.
② 외부 인프라가 없어도 검증 가능한 규칙은 외부 시스템에 의존하지 않게 유지한다.
