# Agent-Polish — ProjectHub Agent 정책

갱신일: 2026-09-26 (KST)

이 문서는 `src/ProjectHub.Agent`의 장기 정책을 정의한다.

제1조 (책임)

① Agent는 개발 PC에서 ProjectHub에 필요한 로컬 상태를 수집하고 Server에 전달한다.
② 현재 책임에는 heartbeat, 등록 프로젝트 상태, Git 상태 및 필요한 로컬 파일 메타데이터 수집이 포함될 수 있다.
③ Agent는 중앙 Server의 저장소 구현이나 Worker의 AI 관제 역할을 대신하지 않는다.

제2조 (의존성)

① Agent는 공통 도메인 계약을 위해 Core를 참조할 수 있다.
② Agent는 Infrastructure의 서버측 저장소 구현에 직접 의존하지 않는 것을 원칙으로 한다.
③ Server와의 통신은 명시된 HTTP 경계를 통해 수행한다.

제3조 (수집 경계)

① Agent는 필요한 로컬 사실을 수집하되 그 사실의 제품 의미를 임의로 확대하지 않는다.
② Git 상태와 파일 메타데이터 수집은 사용자 작업 내용을 자동 변경하지 않는다.
③ 대용량 파일의 로컬 조사와 전송 준비는 원본 파일의 안전을 우선한다.

제4조 (설정과 장애)

① Server 주소, workstation 식별자와 등록 프로젝트 정보는 런타임 설정에서 읽는다.
② 네트워크 장애는 로컬 상태를 임의로 성공 처리하지 않고 기술적 실패로 기록한다.
③ 종료 요청과 취소 신호를 존중해 백그라운드 수집을 중단할 수 있어야 한다.
