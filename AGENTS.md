# ProjectHub 저장소 작업 계약

이 문서는 저장소에서 코드를 수정하는 AI가 항상 지켜야 하는 최소 공통 불변식만 정의한다. 과거 작업 문서·계획·로그·인수인계 파일은 사용자가 명시적으로 파악 또는 조사를 요청한 경우에만 확인한다.

제1조 (작업 범위)

① 현재 사용자 요청과 현재 작업에 필요한 파일만 확인한다.
② 새 작업에서 과거 계획, 작업 기록, 로그와 세션 상태를 자동으로 조사하거나 문맥으로 복구하지 않는다.
③ 특정 작업에만 필요한 임시 규칙을 저장소 전역 규칙으로 확대하지 않는다.

제2조 (프로젝트 경계)

① Core는 공통 도메인과 계약을 담당한다.
② Infrastructure는 외부 시스템 구현을 담당한다.
③ Server는 중앙 HTTP 서비스를 담당한다.
④ Agent는 개발 PC 상태 수집과 Server 통신을 담당한다.
⑤ Worker는 AI 관제와 로컬 작업 실행을 담당한다.
⑥ Web 확장은 ChatGPT Web과 Worker 사이의 브리지를 담당한다.
⑦ 한 프로젝트의 수정으로 다른 프로젝트 책임을 암묵적으로 흡수하지 않는다.

제3조 (구현 및 검증)

① 요청 범위에 필요한 최소 변경을 우선한다.
② 변경 규모에 맞는 빌드·테스트를 실행하고 실제 결과만 기록한다.
③ 실행 환경에 필요한 도구가 없으면 미실행 사실과 가능한 정적 검증 결과를 구분해 기록한다.
④ 장애 대응 규칙은 관측된 실패 문장을 복사해 일반화하지 말고 재사용 가능한 불변식으로 최소화한다.

제4조 (Git 및 보안)

① Git 충돌, detached HEAD, dirty pull과 진행 중인 rebase를 의미 판단으로 자동 해결하지 않는다.
② 명시적 승인 없이 commit, push, 배포 또는 외부 시스템 변경을 수행하지 않는다.
③ 비밀키, 자격증명, 토큰, 결제정보와 민감한 URL을 코드·문서·로그에 남기지 않는다.
④ Supabase 서비스 역할 키는 서버 환경 변수에만 둔다.

제5조 (문서)

① 규범 문서는 조, 항, 호 구조의 계약서 형식을 사용한다.
② 특정 사용자 요청, 테스트 시나리오, 도메인 사례와 일회성 장애 대응을 장기 정책으로 승격하지 않는다.
③ 정책 문서, 작업 문서와 AI 역할 프롬프트의 설명 문장은 한글로 작성한다.
④ ACTION, GOTO, QID, 상태 코드, 클래스명, 파일명, 명령어와 API 필드명처럼 상호 운용 또는 코드 식별에 필요한 토큰은 원형을 유지할 수 있다.
