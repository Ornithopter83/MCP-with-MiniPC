# ProjectHub 저장소 작업 계약

이 문서는 저장소에서 코드를 수정하는 AI가 항상 지켜야 하는 최소 불변식만 정의한다. 과거 작업 문서·계획·로그·인수인계 파일은 사용자가 명시적으로 파악 또는 조사를 요청한 경우에만 확인한다.

제1조 (작업 범위)

① 현재 사용자 요청과 현재 작업에 필요한 파일만 확인한다.
② 새 작업에서 과거 작업 문서, 계획, 로그, 세션 상태를 자동으로 조사하거나 문맥으로 복구하지 않는다.
③ 다른 정책·피드백·활성 작업 문서를 선행 조건으로 자동 참조하지 않는다.
④ 특정 작업에만 필요한 임시 규칙을 저장소 전역 규칙으로 확대하지 않는다.

제2조 (Worker 경계)

① Worker는 흐름·상태·세션·전송·프로세스·기계 계측을 관리하는 기계적 실행 계층이다.
② Worker는 요청 의도, 우선순위, 요구사항 충족, 테스트 충분성, JUDGE 결과, RESOURCE 품질을 의미적으로 판단하지 않는다.
③ 프로토콜, 스키마, 시간 초과, 취소, 인증, 경로 안전성, 프로세스 종료처럼 기계적으로 확인 가능한 사실만 Worker가 판정한다.
④ AI 본문 내용이나 출처 문자열을 의미적으로 해석해 라우팅을 결정하지 않는다.

제3조 (역할 및 제어 불변식)

① 역할은 HQ, WORK, RESOURCE, JUDGE와 기계적 오류 상태로 구분한다.
② 역할 전환은 정의된 ACTION/GOTO 제어와 전송 스키마로만 처리한다.
③ 역할 프롬프트의 대괄호 표기는 실제 ACTION/GOTO 같은 제어 토큰에만 사용한다.
④ RESOURCE 생성 결과의 미적·기능적 품질과 JUDGE 결과의 의미 해석은 Worker가 대신 판단하지 않는다.
⑤ 비동기 계측 결과의 의미와 후속 조치는 WORK 또는 HQ가 판단한다.

제4조 (구현 및 검증)

① 요청 범위에 필요한 최소 변경을 우선한다.
② 변경 규모에 맞는 빌드·테스트를 실행하고 실제 실행 결과만 기록한다.
③ 실행 환경에 필요한 도구가 없으면 미실행 사실과 가능한 정적 검증 결과를 구분해 기록한다.
④ 장애 대응 규칙은 관측된 실패 문장을 복사해 일반화하지 말고 재사용 가능한 기계적 불변식으로 최소화한다.

제5조 (Git 및 보안)

① Git 충돌, detached HEAD, dirty pull, 진행 중인 rebase를 의미 판단으로 자동 해결하지 않는다.
② 명시적 승인 없이 commit, push, 배포 또는 외부 시스템 변경을 수행하지 않는다.
③ 비밀키, 자격증명, 토큰, 결제정보, 민감한 URL을 코드·문서·로그에 남기지 않는다.
④ Supabase 서비스 역할 키는 서버 환경 변수에만 둔다.

제6조 (문서 및 계약)

① 역할 계약에는 장기간 유지되는 역할 책임, 제어 문법, 전송 형식, 기계적 경계만 둔다.
② 특정 사용자 요청, 테스트 시나리오, 도메인 사례, 일회성 장애 대응은 역할 계약에 넣지 않는다.
③ 역할 계약, 정책 문서, 작업 문서와 AI 역할 프롬프트의 설명 문장은 한글로 작성한다.
④ ACTION, GOTO, QID, 상태 코드, 클래스명, 파일명, 명령어, API 필드명처럼 상호 운용이나 코드 식별에 필요한 토큰은 원형을 유지할 수 있다.

제7조 (프로젝트 기준)

① ProjectHub v0.2의 개발 PC 에이전트 → Mini PC 서버 → Supabase 흐름을 보존한다.
② 구현 변경은 기존 역할 분리와 Worker 비판단 원칙을 깨지 않는 범위에서 수행한다.
