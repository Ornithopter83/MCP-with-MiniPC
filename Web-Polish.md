# Web-Polish — ProjectHub Web 확장 정책

갱신일: 2026-09-26 (KST)

이 문서는 `extension/gptweb-hub`의 장기 정책을 정의한다.

제1조 (책임)

① Web 확장은 ChatGPT Web 대화와 로컬 Worker 사이의 브리지 역할을 수행한다.
② 관리형 브라우저에서는 Worker가 지정한 HQ 또는 RESOURCE 슬롯 역할을 사용해 현재 대화를 자동 연결하며, 수동 브라우저 호환 모드에서는 사용자가 역할을 명시적으로 연결할 수 있다.
③ 확장은 ProjectHub의 Core 도메인, Server 중앙 서비스 또는 Agent 상태 수집 책임을 대신하지 않는다.

제2조 (연결 경계)

① HQ와 RESOURCE는 서로 다른 conversationId에 연결한다.
② heartbeat는 연결 생존과 확장 상태 확인에 사용하며 작업 목적지의 의미를 결정하지 않는다.
③ 로컬 브리지 통신 대상은 127.0.0.1 또는 localhost 범위로 제한한다.

제3조 (작업 전달)

① 일반 HQ Web 작업은 Worker가 지정한 대화에서 텍스트 결과를 수집해 반환한다.
② RESOURCE 작업은 Worker가 전달한 자연어 요청을 ChatGPT Web에 보내고 생성된 파일 결과를 기계적으로 수집한다.
③ RESOURCE 결과는 파일명, MIME 형식과 바이트를 포함하는 공통 결과 구조로 Worker에 전달할 수 있다.
④ 확장은 생성 결과가 어느 코드나 컴포넌트에 적합한지 의미적으로 판단하지 않는다.

제4조 (생성 파일 수집)

① 생성 파일 탐지는 이미지, 첨부, 다운로드 링크, 오디오·비디오 등 실제로 수집 가능한 결과를 대상으로 한다.
② CORS 또는 브라우저 권한 문제로 콘텐츠 스크립트가 직접 수집할 수 없는 허용된 ChatGPT/OpenAI 파일은 백그라운드 확장이 기계적으로 재시도할 수 있다.
③ 생성 파일의 최종 저장 경로와 경로 안전성 검사는 Worker 책임으로 둔다.

제5조 (안전과 호환성)

① 브라우저 확장 권한은 Worker 브리지와 필요한 ChatGPT/OpenAI 파일 수집 범위로 제한한다.
② 전송 상태와 다운로드 진행 사실은 관측 정보로 전달하되 작업 의미를 판정하지 않는다.
③ 레거시 호환 처리는 신규 프로토콜의 책임 경계를 침범하지 않는 범위에서 유지할 수 있다.

제6조 (관리형 브라우저 모드)

① 관리형 브라우저 모드에서는 확장 프로그램의 시각 패널을 사용자에게 표시하지 않고 bridge 기능만 실행한다.
② 관리 역할은 HQ 또는 RESOURCE 중 하나이며 해당 persistent profile에 저장할 수 있다.
③ 로그인 뒤 사용자가 ChatGPT 대화를 선택하면 현재 conversationId를 지정된 역할에 자동 연결한다.
④ 관리형 HQ와 RESOURCE는 서로 다른 브라우저 프로필과 서로 다른 conversationId를 사용한다.
⑤ 메시지 전송, assistant 응답 탐지, streaming 종료와 응답 안정화는 관측 가능한 Web 상태를 이용해 기계적으로 확인한다.
⑥ 기존 assistant DOM이 재사용되는 경우 현재 Worker 메시지의 실제 전송이 확인된 뒤 assistant 텍스트 변화도 새 응답의 기계적 증거로 사용할 수 있다.
⑦ Worker에서 Web으로 보내는 첨부 파일과 RESOURCE가 Worker로 반환하는 파일은 가능한 경우 SHA-256을 함께 전달하고 양쪽에서 다시 계산해 불일치를 실패로 처리한다.
⑧ 확장은 파일의 내용 적합성이나 생성 품질을 판단하지 않는다.

